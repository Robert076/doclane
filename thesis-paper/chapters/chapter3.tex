\chapter{Implementation}
\label{chap:ch3}

\par Doclane's backend is written in Go and the frontend is built with Next.js. Go was chosen primarily for its performance characteristics and its approach to error handling, which makes tracing bugs through a multi-layered application considerably more straightforward than in many dynamically typed alternatives. Next.js was selected because the application is data-heavy by nature, with nearly every page requiring fresh information from the backend, and server-side rendering reduces the time users spend waiting for content to appear.

\section{Backend}

\subsection{Architecture and layering}

\par The backend is organized into four distinct layers, each with a clearly defined responsibility. The handler layer sits closest to the HTTP boundary and is responsible for extracting what each request carries, whether that is JWT claims, resource identifiers, or uploaded files. It does not make judgments about whether an operation is permitted; that responsibility belongs to the service layer, which contains the business logic and enforces rules such as whether a user holds the right role to access a particular resource. As a concrete example, a user cannot view files attached to a request unless they are an administrator or belong to the department that manages it.

\par Below the service layer sits the repository layer, which executes database queries and operates on the assumption that everything passed to it has already been validated. When an action involves multiple database operations that must all succeed or all fail together, the repository uses transactions to enforce atomicity. Finally, the model layer defines the data structures shared across the application, including users, requests, templates, invitation codes, and comments. In cases where the raw database records do not contain all the information a caller needs, DTOs (Data Transfer Objects) are used to enrich the response, for instance, attaching a requester's name and email address alongside their identifier when a request is retrieved.

\subsection{Authentication}

\par Authentication is handled through JWTs (JSON Web Tokens) issued by AWS Cognito. A JWT encodes information about the authenticated user, including their identifier, role, department, and display name, in a format that is readable by any party but cannot be tampered with without invalidating the cryptographic signature the token carries. The backend verifies this signature on every protected request and rejects any token that does not pass validation.

\par Tokens are stored in HTTP-only cookies, which keeps them inaccessible to JavaScript running in the browser and removes a common vector for XSS (Cross-Site Scripting) attacks. The configuration of Cognito itself is covered in more detail in chapter 4.

\subsection{API response structure}

\par All responses from the API follow a uniform structure, which simplifies how the frontend handles them. Every response includes a \textbf{success} boolean indicating whether the operation completed without error, an optional \textbf{message} field with a short description of the outcome, an optional \textbf{error} field populated when success is false, and an optional \textbf{data} field carrying any payload the request was meant to return.

\begin{center}
    \includegraphics[width=0.8\textwidth]{figures/api-response.png}
    \captionsetup{type=figure}
    \captionof{figure}{API Response struct.}
\end{center}

\par Custom error types are defined across the backend to complement this structure. Rather than passing raw error strings through the call stack, typed errors give each failure a consistent shape, which makes both the code easier to follow and the responses more predictable for the client.

\subsection{Configuration, middleware, and routing}

\par Application setup is centralised in a single configuration file where all service and repository instances are created. This means that swapping out a repository implementation, for example to use a different database driver, only requires a change in one place; nothing in the handler or service layers needs to be touched. Most routes are protected by middleware that runs before any handler logic executes. Doclane uses two middleware checks in sequence: the first verifies that the incoming token was signed correctly by Cognito, and the second confirms that the associated user account is currently active. Requests that fail either check are rejected before they can reach application logic.

\par HTTP routing is handled using the Chi library rather than Go's standard library router. While the standard library covers most needs adequately, routing is one area where a third-party package noticeably improves clarity and organisation.

\begin{center}
    \includegraphics[width=0.8\textwidth]{figures/implementation/backend/chi-routes.png}
    \captionsetup{type=figure}
    \captionof{figure}{Routing for the backend.}
\end{center}

\subsection{File storage}

\par Uploaded files are stored in AWS S3. The file storage logic is deliberately kept behind an abstraction layer so that the rest of the application does not depend on S3 directly; the storage provider could be replaced without touching handler or service code. File access is granted through presigned URLs, which are time-limited links that allow a client to retrieve or upload a specific object without holding permanent credentials. The S3 setup is described in more detail in chapter 4.

\section{Database}

\par PostgreSQL was chosen as the database engine. The data in Doclane has clear, well-defined relationships between entities, requests, documents, users, departments, and templates, which makes a relational model a natural fit. PostgreSQL is also one of the more mature and widely deployed options available, which reduces operational risk.

\begin{center}
    \includegraphics[width=0.8\textwidth]{figures/implementation/backend/database-diagram.png}
    \captionsetup{type=figure}
    \captionof{figure}{Database schema overview.}
\end{center}

\section{Frontend}

\par The frontend is built with Next.js, a React-based framework that supports server-side rendering. The decision to use it rather than a client-side-only approach comes down to the nature of the application: because almost every page in Doclane depends on data fetched from the backend, a purely client-side setup would mean users frequently encounter loading states while data is retrieved after the page has already been painted. By fetching data on the server before the response is sent to the browser, Next.js eliminates most of that latency from the user's perspective.

\par On the component level, the frontend follows the DRY (Don't Repeat Yourself) principle by breaking the interface into small, reusable pieces. Any UI element that appears in more than one place is defined once and composed where needed, which keeps the codebase consistent and reduces the surface area for visual inconsistencies as the application evolves.